Stop. I don't need FDIC bank data. I need you to modify parser.py to extract credit card numbers from the files already in the `matches` folder.

Do not download anything. Do not fetch external data. Only modify parser.py to add:

1. Regex patterns for 15-16 digit card numbers (with or without dashes/spaces)
2. Luhn algorithm validation to filter valid cards
3. BIN extraction (first 6 digits)
4. Card brand detection (Visa, Mastercard, Amex, Discover) based on BIN

Add these new fields to the output: `CardNumber`, `BIN`, `CardBrand`.

Then re-run `parser.py` on the existing `matches` folder and show me the new output.

Execute now.
