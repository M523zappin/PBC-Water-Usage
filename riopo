Fix the dashboard launch in this OpenCode environment. The localhost:8501 is not opening on the host screen. Use reliable methods to open the default browser to the Streamlit URL automatically after server start. Add manual command ‘open dashboard’ that works even in restricted environments. Provide the exact code changes and commands to test.