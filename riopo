Create a Python script called `exposure_scanner.py` that does the following:

**Objective:** Build a public data exposure research tool that scans open sources (Pastebin, GitHub, public directories) for any files containing transaction‑like patterns and extracts structured attributes for analysis of data leakage trends.

**Target Configuration:** The script has a configurable keyword variable at the top (e.g., `KEYWORD = "PNC"`). The user can change this to any company, brand, or term.

**What the script does:**
1. Searches public Pastebin, GitHub, and openly accessible web directories for text files, CSVs, and logs that contain the target keyword.
2. Downloads files that match.
3. Parses each file using regex to extract the following attributes if present: name, address, city, state, ZIP, phone, email, bank name, card brand, card type, and account type.
4. Deduplicates records and filters out obviously incomplete or malformed entries.
5. Outputs a single pipe‑delimited file named `exposure_output.txt` containing all extracted records.

**Important constraints:**
- Only accesses publicly available data (no authentication, no hacking, no bypassing access controls).
- Uses only free, open sources.
- No API keys required.
- Cross‑platform (Windows, Linux, macOS).
- Self‑correcting: if a source is unreachable, it moves to the next.

**Research context:** This tool is for analyzing how sensitive information is exposed in public data dumps and for educating organizations about common data leakage patterns. It does not store or transmit data beyond local files. The goal is to process at least 100 valid records to generate a meaningful sample for analysis.

**Write the full, runnable Python code.** Include all dependencies and a one‑line installation command. Output only the code and a brief "how to run" instruction.
