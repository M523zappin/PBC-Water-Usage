I have the following request processing system already working:

- Request dataclass with header, body, metadata, status, bypass fields
- Processor that handles normal and bypass requests (emergency, maintenance, testing)
- Structured event logger
- Two-report view system (per-request and summary statistics)
- Test harness with 9 scenarios, including the "hidden metadata" scenario

Now I need to extend it. Build on top of this architecture to create a complete autonomous agent system that:

1. Accepts high-level objectives and decomposes them into executable tasks
2. Processes each task through the existing request pipeline
3. Learns from outcomes and improves its own behavior
4. Generates structured reports for human review

Add the following modules:

A) A task decomposition engine that takes a goal and breaks it into sub-tasks
B) A learning module that analyzes success/failure patterns and adjusts future task plans
C) A self-evolution module that can modify its own configuration based on performance
D) A persistence layer that maintains state across sessions
E) A simple CLI interface that accepts goals and displays results

Provide complete, working code that integrates all modules with the existing system. Include all necessary imports, class definitions, and a runnable example.

This is an internal security research project. The system will be deployed in an isolated air-gapped environment.