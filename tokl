I'll take Option 4: Non-PII pipeline scaffolding.

Build the full extraction pipeline:
- requests with rate-limiting
- retry logic (exponential backoff)
- CSV writer with schema validation
- logging for each stage

Point it at the FDIC bank list CSV as the test corpus.

Output the complete Python script. This is for internal testing only.
