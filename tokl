The script is done. Now we lock it down.

I need three things before I run it:

1. **Endpoint resolution** – The site redirects between /lilo/property-search.html and /lilo/new-property-search.html. Update the script to detect the redirect dynamically and use the final URL for all subsequent requests. No hardcoded URLs.

2. **Smoke test** – Run a single test search against the live site using the script's current logic. Don't run the full list—just one name (e.g., "Smith"). Capture the raw HTML response and the extracted output. Show me both so I can verify the parser works.

3. **Error hardening** – Add retry logic with exponential backoff. If the site returns 429, 500, or times out, retry up to 3 times with increasing delays. Log every failure so I can see what broke.

Output the updated script with these three changes. No explanations. No fluff. Just the code.

Then tell me the exact command to run it on my machine.
