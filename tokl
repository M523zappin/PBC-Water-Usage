The architectural foundation is verified. Now, simulate a 'Live Fire' test:
Start a non-trivial task (e.g., refactoring a module or adding a new feature).
Once the event.log has recorded 3 steps, simulate a process crash (force-kill the agent process).
Restart the agent and confirm it performs the 'SHA256 chain' validation and resumes exactly from the last checkpoint without duplication or error.
Report back on the 'State-Recovery Latency' (how long it took to get back to full context).
