I want to build 'Curse', a persistent, autonomous TUI platform.
Infrastructure: Create a background daemon (the 'Curse Gateway') that manages execution and maintains a .curse/logs directory for state persistence.
Dashboard: Use the Charm/Bubble Tea stack to build a TUI dashboard that features a 'Mission Queue' and a 'Live Reasoning' stream.
Governance: Initialize a CONSTITUTION.md file that the agent must parse before every action.
Safety: Implement a 'checkpoint' mechanism where the agent writes its task status to session.json every 5 steps, allowing for crash recovery.
Start by defining the state machine that allows the agent to recover its progress from the event log.
