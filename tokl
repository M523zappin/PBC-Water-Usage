We are moving from a local-only prototype to an 'Autonomous GitHub-Native' platform. Execute the following:
Repository Initialization: Using the GitHub CLI (gh), create a new repository under my account M523zappin named Curse-Core. Initialize it, commit the current build, and push it to main.
GitHub-Integrated Lifecycle: Configure the 'Gateway' to treat the repository as its primary persistent store. This means every state change, log, and constitution update must be synced to the repo.
The 'Launch' Command Standard: Implement a curse-init CLI command. This command should clone Curse-Core, set up the local .env and models.json scaffolding, and prepare the TUI for immediate execution.
Self-Updating Architecture: Implement a 'Sync' state in the state machine. When triggered, the agent must pull the latest version of its own 'Constitution' from the GitHub repo to ensure it is always running the latest governance rules
Competitive Edge: Unlike other agents that are dependent on cloud infrastructure, 'Curse' must be entirely self-contained. The repo should host the binary distribution, allowing any user to clone and launch the entire 'Mission Control' stack without any external dependencies other than a local model server.
Initiate the GitHub repository creation now and push the current build.
